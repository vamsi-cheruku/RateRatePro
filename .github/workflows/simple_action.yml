name: pjn is trying to setup CICD

on:
  push

jobs:
  trail-job-run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: prinitng step
        run: |
          ls -al
  trail-job-run2:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: 2nd prinitng step 
        run: |
          dir
